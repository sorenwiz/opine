<%= render 'refinery/admin/search_header', :url => refinery.vote_options_admin_vote_options_path %>
<div class='pagination_container'>
  <% if @vote_options.any? %>
    <%= render 'vote_options' %>
  <% else %>
    <p>
      <% if searching? %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% else %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% end %>
    </p>
  <% end %>
</div>
