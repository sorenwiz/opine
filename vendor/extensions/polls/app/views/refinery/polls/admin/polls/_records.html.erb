<%= render 'refinery/admin/search_header', :url => refinery.polls_admin_polls_path %>
<div class='pagination_container'>
  <% if @polls.any? %>
    <%= render 'polls' %>
  <% else %>
    <p>
      <% if searching? %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% else %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% end %>
    </p>
  <% end %>
</div>
