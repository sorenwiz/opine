section.wrapper
  .container
    h2 Afstemninger
    - 2.times
      .card-deck-wrapper
        .card-deck
          = render partial: 'refinery/polls/poll', collection: @polls.pop(3)

section.wrapper.section-polls
  .container
    h2.section-polls-title
      span.fa.fa-fw.fa-line-chart>
      | Aktuelle afstemninger
    .card-deck-wrapper
      .card-deck
        = render partial: 'refinery/polls/poll', collection: @polls.pop(3)
