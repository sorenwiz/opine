<% content_for :body do %>
  <ul id="polls">
    <% @polls.each do |poll| %>
      <li>
        <%= link_to poll.slug, refinery.polls_poll_path(poll) %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render '/refinery/content_page' %>
